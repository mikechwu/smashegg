{
  "$schema-note": "The MACHINE SOURCE OF TRUTH for the layout model. status/MODEL.md is generated from this file by scripts/gen-model.mjs and must never be hand-edited. Every entry's `source` names where the value physically lives; tests/unit/client/model-drift.test.ts asserts that those sources still contain it, so this file cannot quietly drift from the code. Provenance longer than one line belongs in status/VALIDATED.md, not here — see that file's row with the same id.",
  "version": 1,
  "reference": {
    "innerH": 664,
    "innerW": 390,
    "note": "The cell every span figure in this model is stated at: inner 390x664, lacquer, zh-Hant, timed room (the product default), one card staged, no shelf, ascending sort. A figure quoted without this cell is a figure quoted without its configuration."
  },
  "constants": [
    {
      "id": "cardW",
      "value": 46.51,
      "unit": "px",
      "what": "The hand card box width below the 720px layout breakpoint. A constant, in a unit the user cannot scale.",
      "source": { "file": "src/client/app.css", "literal": "--gd-handcardw: 46.51px" },
      "validated": "cardW"
    },
    {
      "id": "glyphCap",
      "value": 56,
      "unit": "px",
      "what": "Ceiling on the card ink basis --gd-glyphw. Above it the corner index and the body pip collide. Measured at the 48.15px box as a first escape at 62px and carried to this box at the same ratio, then re-measured.",
      "source": { "file": "src/client/app.css", "literal": "min(2.906875rem, 56px)" },
      "validated": "glyphCap"
    },
    {
      "id": "aspect",
      "value": 1.45,
      "unit": "ratio",
      "what": "Card height as a multiple of card width.",
      "source": { "file": "src/client/table/table.css", "literal": "calc(var(--gd-cardw) * 1.45)" },
      "scriptValue": 1.4497041420118344,
      "scriptNote": "The gate scripts use 73.5/50.7 = 1.44970, which is the MEASURED height at the old card divided by that card. The two differ by 0.0003, i.e. 0.03px of span at a 48.15px card, because 73.5 is itself a rounded reading of 50.7 x 1.45 = 73.515. The CSS value is authoritative; the scripts have not been changed to it this round because doing so would move every recorded gate figure by a fraction of a pixel for no gain.",
      "validated": "aspect"
    },
    {
      "id": "stripW",
      "value": 0.42,
      "unit": "fraction of cardW",
      "what": "Visible top-edge fraction of each non-base card in a stack — the lattice step is stripW x cardW.",
      "source": { "file": "src/client/table/themes/lacquer.tsx", "literal": "stackStripW: 0.42" },
      "variants": { "lacquer": 0.42, "cinnabar-court": 0.841 },
      "variantNote": "THIS IS A LACQUER CONSTANT AND THE MODEL IS A LACQUER MODEL. The second deck theme sets stackStripW: 0.841, almost exactly twice, so its lattice step and therefore its fan height are roughly double at the same card width. Every span, threshold, margin and rate in this file is stated at lacquer and does not transfer. The pointer here was originally written as `theme.ts`, where only the TYPE lives; the drift test rejected it, which is how the second value was found at all.",
      "validated": "stripW"
    },
    {
      "id": "fanChrome",
      "value": 13.9,
      "unit": "px",
      "what": "The fan's own padding. Not card-scaled.",
      "source": { "file": "scripts/cardw-gate.mjs", "literal": "CHROME = 13.9" },
      "validated": "fanChrome"
    },
    {
      "id": "fanRowGap",
      "value": 6.0,
      "unit": "px",
      "what": "Row gap between the fan's two visual lines.",
      "source": { "file": "scripts/cardw-gate.mjs", "literal": "GAP = 6.0" },
      "validated": "fanRowGap"
    },
    {
      "id": "rowChrome",
      "value": 48.0,
      "unit": "px",
      "what": "Horizontal chrome around a fan line: contentW = W - rowChrome - 0.3*cardW.",
      "source": { "file": "scripts/cardw-gate.mjs", "literal": "ROW_CHROME = 48.0" },
      "validated": "rowChrome"
    },
    {
      "id": "deskMinusCard",
      "value": 83.0,
      "unit": "px",
      "what": "Desk height less the one card height it contains: deskH = deskMinusCard + aspect*cardW.",
      "parts": { "titleRow": 27, "bar": 4, "status": 24, "gaps": 12, "padding": 14, "border": 2 },
      "partsNote": "bar = 4 is TIMED-ONLY; removing it and its gap gives the untimed 148.5px desk at the old card.",
      "source": { "file": "scripts/cardw-gate.mjs", "literal": "DESK_MINUS_CARD = 83.0" },
      "validated": "deskMinusCard"
    },
    {
      "id": "kMinusCard",
      "value": 125.1,
      "unit": "px",
      "what": "Everything in the span that is neither fan nor desk, less the one card height inside the trick well: K_well = kMinusCard + aspect*cardW.",
      "parts": { "wellToFanBand": 59, "fanToDesk": 10, "deskToBar": 15, "barHeight": 41 },
      "partsNote": "The parts sum to 125.0 against a total of 125.1, a residual of 0.1px. The round that recorded this decomposition called it exact; it is not, and the discrepancy was found by putting the parts and the total in one machine-checked place. 0.1px does not move any decision in this model — it is 0.02% of the span — but 'residual 0px' is a stronger claim than the numbers support.",
      "source": { "file": "scripts/cardw-gate.mjs", "literal": "K_MINUS_CARD = 125.1" },
      "validated": "kMinusCard"
    },
    {
      "id": "kLead",
      "value": 66.0,
      "unit": "px",
      "what": "K when the viewer LEADS and the trick well is empty. Carries no card term — the card in K_well is inside the well.",
      "source": { "file": "scripts/fan-height-distribution.mjs", "literal": "K_LEAD = 66.0" },
      "validated": "kLead"
    },
    {
      "id": "revealBudget",
      "value": 2.95,
      "unit": "card widths",
      "what": "Total reveal a stacked column may spend: stackOffsetW(n, strip) = min(strip, 2.95/(n-1)), so a column of n reveals min(strip*(n-1), 2.95). It binds at n >= 5 for a 0.841 strip and NEVER for 0.42, since a value class holds at most 8 copies and 0.42*7 = 2.94.",
      "source": { "file": "src/client/table/HandFan.tsx", "literal": "Math.min(stripW, 2.95 / Math.max(n - 1, 1))" },
      "validated": "revealBudget"
    },
    {
      "id": "maxValueClasses",
      "value": 15,
      "unit": "count",
      "what": "12 non-level natural ranks + the level class + small joker + big joker. A class present in the hand is exactly one fan column.",
      "source": { "file": "scripts/cardw-gate.mjs", "literal": "MAX_CLASSES = 15" },
      "validated": "maxValueClasses"
    },
    {
      "id": "maxColumnDepth",
      "value": 8,
      "unit": "cards",
      "what": "The deepest a single fan column can ever be. A column is one value class, and a class holds 8 copies: two decks x four suits. Structural — a counting argument, not a measurement.",
      "source": { "file": "scripts/theme-bands.mjs", "literal": "CLASS_SIZES = [...Array(12).fill(8), 8, 2, 2]" },
      "validated": "maxColumnDepth"
    },
    {
      "id": "capacityFloor",
      "value": 8,
      "unit": "columns",
      "what": "Per-line capacity needed to fit 15 value classes in TWO lines.",
      "source": { "file": "scripts/containment.mjs", "literal": "capacity < 8" },
      "validated": "capacityFloor"
    },
    {
      "id": "layoutBreakpoint",
      "value": 720,
      "unit": "px",
      "what": "The phone/desktop layout seam. The card constant governs below it; above it the card is in rem again.",
      "source": { "file": "src/client/table/table.css", "literal": "@media (min-width: 720px)" },
      "validated": "layoutBreakpoint"
    },
    {
      "id": "floorCardW",
      "value": 44,
      "unit": "px",
      "what": "The hand card box below the crossover, where the constant cannot fit 8 columns a line. Exactly what today's clamp yields at 320 through its rem floor.",
      "source": { "file": "src/client/app.css", "literal": "--gd-handcardw: 44px" },
      "validated": "floorCardW"
    },
    {
      "id": "floorBelowWidth",
      "value": 332,
      "unit": "px",
      "what": "Viewport width at and below which the narrow floor applies. The last integer below the 332.1px crossover.",
      "source": { "file": "src/client/app.css", "literal": "@media (max-width: 332px)" },
      "validated": "floorBelowWidth"
    },
    {
      "id": "depthFloor",
      "value": 10,
      "unit": "bin index",
      "what": "The minimum fan depth that must remain feasible. A PRODUCT POLICY, not a validation result: the held-out test earns 9, and requiring 10 is a stricter claim the owner makes about which depths are in scope.",
      "source": { "file": "scripts/cardw-gate.mjs", "literal": "DEPTH_FLOOR ?? 10" },
      "validated": "depthFloor"
    },
    {
      "id": "minGuaranteedWidth",
      "value": 360,
      "unit": "px",
      "what": "Smallest viewport width the card constant is guaranteed at. Below the crossover the layout has no supported card size.",
      "source": { "file": "scripts/cardw-gate.mjs", "literal": "MIN_GUARANTEED_W ?? 360" },
      "validated": "minGuaranteedWidth"
    }
  ],
  "formulas": [
    {
      "id": "capacity",
      "expression": "capacity(W, w) = floor((W - rowChrome - 0.3*w) / (0.7*w))",
      "source": { "file": "scripts/cardw-gate.mjs", "literal": "Math.floor(contentFor(W, w) / (0.7 * w))" },
      "what": "Columns that fit on one fan line at viewport width W and card width w. Monotonically non-decreasing in W at fixed w."
    },
    {
      "id": "capacityCeiling",
      "expression": "maxCardW(W) = (W - rowChrome) / (0.7*capacityFloor + 0.3) = (W - 48.0) / 5.9",
      "source": { "file": "scripts/cardw-gate.mjs", "literal": "(W - ROW_CHROME) / (0.7 * 8 + 0.3)" },
      "what": "Largest card width that still clears the two-line capacity floor at width W."
    },
    {
      "id": "crossover",
      "expression": "minWidth(w) = rowChrome + 5.9*w",
      "source": { "file": "scripts/cardw-gate.mjs", "literal": "ROW_CHROME + 5.9 * CONSTANT" },
      "what": "Smallest viewport width at which card width w still clears the capacity floor. At the shipped 48.15px this is 332.1 CSS px."
    },
    {
      "id": "lineHeight",
      "expression": "lineH(d, w) = aspect*w + stripW*w*(d - 1)",
      "source": { "file": "scripts/fan-height-distribution.mjs", "literal": "GEOM.CARD_H + GEOM.STEP * (d - 1)" },
      "what": "Height of one fan line whose deepest column holds d cards."
    },
    {
      "id": "fanHeight",
      "expression": "fanH(d1, d2, w) = fanChrome + fanRowGap + 2*aspect*w + w*(reveal(d1) + reveal(d2)),  reveal(n) = min(stripW*(n - 1), revealBudget)",
      "source": { "file": "scripts/cardw-gate.mjs", "literal": "CHROME + GAP + 2 * ASPECT * w + STRIP * w * (s - 2)" },
      "what": "Two-line fan height. It collapses to the simpler `fanChrome + fanRowGap + 2*aspect*w + stripW*w*(s-2)` — a function of s = d1 + d2 alone — EXACTLY WHEN THE REVEAL BUDGET DOES NOT BIND, which for stripW 0.42 is always, since the deepest possible column is 8 copies and 0.42*7 = 2.94 < 2.95. That is why the gate script carries the collapsed form and why every lacquer figure in this project is unaffected. For a theme whose strip reaches the budget the collapsed form OVERSTATES the height, so a RATE computed from it is wrong: feasibility stops being a function of s alone, and (5,1) and (4,2) are both s=6 and can land on opposite sides. THE GATE IS UNAFFECTED, AND THAT IS NOT LUCK. reveal() is concave in depth, so at fixed s the sum is maximised by the BALANCED split, and at the balanced split it equals the collapsed form whenever that split does not itself reach the budget — which makes the collapsed form the exact MAXIMUM over splits, which is what a bound needs. Verified by brute force over every achievable (d1, d2) at every s: the balanced split was the maximiser in all 45 cases. So the marginal-bin framing holds wherever the balanced split at the depth floor stays under the budget, including lacquer at every depth the shoe allows, the shipped strip ceiling, and even cinnabar-court at its own marginal bin. What genuinely needed the capped form was the RATE, which is what the 51.3% -> 50.3% and 66.9% -> 66.6% corrections were. Found by a per-deal point-prediction test against 16 measured span deltas — the collapsed form was off by a full lattice step on every deal holding a depth-5 column, the full form fits all 16 within 0.10px."
    },
    {
      "id": "threshold",
      "expression": "T(w) = innerH - (deskMinusCard + aspect*w) - (kMinusCard + aspect*w) = 455.9 - 2.90*w",
      "source": { "file": "scripts/cardw-gate.mjs", "literal": "INNER_H - (DESK_MINUS_CARD + ASPECT * w) - (K_MINUS_CARD + ASPECT * w)" },
      "what": "The tallest fan that still fits at the reference inner height, following state. Both the desk and K carry one card height each, which is why the coefficient is twice the aspect."
    },
    {
      "id": "margin",
      "expression": "margin(s, w) = T(w) - fanH(s, w) = 436.0 - (4*aspect + stripW*(s - 2))*w",
      "source": { "file": "scripts/cardw-gate.mjs", "literal": "thresholdFor(w) - fanFor(s, w)" },
      "what": "Slack for a hand of total depth s at card width w. Width-independent: the viewport's width enters the gate only through capacity."
    },
    {
      "id": "marginalBinBand",
      "expression": "marginal bin >= K  <=>  margin(K, w) >= 0  <=>  w <= 436.0 / (4*aspect + stripW*(K - 2))",
      "source": { "file": "scripts/cardw-gate.mjs", "literal": "436.0 / (4 * ASPECT + STRIP * (bin - 2))" },
      "what": "The marginal bin is a step function of the CARD ALONE — the viewport width does not appear. Bands at the reference height: bin 11 for w <= 45.52, bin 10 for w <= 47.60, bin 9 for w <= 49.89, bin 8 for w <= 52.41. This is the gate's vertical term, and it replaced a floor on the marginal bin's own slack, which was anti-correlated with the failure rate across a band edge."
    },
    {
      "id": "stripCeiling",
      "expression": "stripW_max(w, K) = (spanBudget/w - 4*aspect) / (K - 2)",
      "source": { "file": "src/client/table/theme.ts", "literal": "stripCeilingFor" },
      "what": "The largest covered-card reveal a theme may request and still fit hands of depth K at card width w. At w = 46.51 and K = 10 this is 0.447. A theme REQUESTS a strip; the framework OWNS this ceiling. Derived rather than stored so it cannot go stale when the card size or the depth floor moves. AND IT IS EXACT, NOT MERELY CONSERVATIVE, which is worth proving here because it is derived from the collapsed fan-height form and that form can fail: at fixed total depth the reveal sum is maximised by the BALANCED split, and at the balanced split the sum equals the collapsed form as long as it does not reach revealBudget. Substituting the ceiling into that check gives a load of (spanBudget/w - 4*aspect)/2 for even K — independent of K entirely — which is 1.79 at the shipped card against a budget of 2.95. It holds for every depth floor K >= 4 at any card wider than 37.27px; it fails only at K = 3, which is not a depth floor anyone would set. Both shipped cards clear it."
    },
    {
      "id": "collapsedExactCeiling",
      "expression": "collapsedExact_max(maxColumnDepth) = revealBudget / (maxColumnDepth - 1)",
      "source": { "file": "src/client/table/theme.ts", "literal": "collapsedExactCeilingFor" },
      "what": "The SECOND strip threshold, and the one the shipped gate does not enforce: 2.95/7 = 0.4214 at maxColumnDepth 8. Above it a theme's deepest columns reach the reveal budget, so every RATE computed for that theme from the collapsed form is wrong even though the depth-floor ceiling (0.447) is still satisfied. A theme at 0.43 passes the gate and breaks its own rates. Exceeding this is LEGAL and must be DETECTED, not refused: it only means that theme's rates need the capped form. Note how tight the exactness is that lacquer relies on: 0.42 sits 0.00143 below this line, 0.34% of its own value."
    },
    {
      "id": "toothBoundary",
      "expression": "w_s = 436.0 / (4*aspect + stripW*(s - 2))",
      "source": { "file": "scripts/cardw-gate.mjs", "literal": "4 * ASPECT + STRIP * (s - 2)" },
      "what": "Card width at which depth-s hands stop fitting. These roots make the qualifying set of card widths DISJOINT, which is why no clamp coefficient passes at every supported width."
    }
  ],
  "shipped": {
    "cardWidthRule": "--gd-handcardw: 44px at and below 332px; 46.51px to 719px; clamp(2.75rem, 13vw, 4.25rem) at and above 720px",
    "glyphRule": "--gd-handglyphw: min(2.75rem, 53px) at and below 332px; min(2.906875rem, 56px) to 719px; the card width at and above 720px",
    "supportedWidths": [360, 375, 390, 430],
    "unsupportedBelow": 320
  }
}
